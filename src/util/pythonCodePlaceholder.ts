export const placeholder = `import datetime

date = datetime.datetime.now().strftime('%Y-%m-%d')
seconds = seconds = datetime.datetime.now().strftime("%S")

json = {
    "current_date": date,
    "current_seconds": seconds
}`;
